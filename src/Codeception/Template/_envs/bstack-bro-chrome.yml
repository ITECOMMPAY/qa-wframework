# BrowserStack Chrome browser config
modules:
    config:
        Codeception\Module\WebTestingModule:
            browser: chrome
            capabilities:
                chromeOptions:
                    args: ['start-maximized']
                browserVersion: 'latest'
