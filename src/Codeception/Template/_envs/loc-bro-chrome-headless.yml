# Local headless Chrome browser config
modules:
    config:
        Codeception\Module\WebTestingModule:
            browser: chrome
            capabilities:
                chromeOptions:
                    args: ["headless"]
