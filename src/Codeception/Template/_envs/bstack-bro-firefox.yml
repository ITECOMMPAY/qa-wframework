# BrowserStack Firefox browser config
modules:
    config:
        Codeception\Module\WebTestingModule:
            browser: firefox
            capabilities:
                browserVersion: 'latest'
