# `local` environment config
modules:
    config:
        Codeception\Module\WebTestingModule:
            browser: chrome
            capabilities:
                chromeOptions:
                args: ["headless"]
