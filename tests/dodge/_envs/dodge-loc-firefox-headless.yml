# `local` environment config
modules:
    config:
        Codeception\Module\WebTestingModule:
            browser: firefox
            capabilities:
                moz:firefoxOptions:
                    args: ["-headless"]
